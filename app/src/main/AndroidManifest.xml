<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.wit.hillfort">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="org.wit.hillfort.main.MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/splash_img"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/splash_img"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="org.wit.hillfort.activities.MapActivity"
            android:label="@string/title_activity_map"></activity>
        <activity android:name=".activities.HillfortActivity"></activity>
        <activity android:name=".activities.HillfortListActivity"></activity>
        <activity android:name="org.wit.hillfort.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>